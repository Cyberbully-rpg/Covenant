# --- Core data / ML ---
pandas==2.2.3
numpy==1.26.4
scikit-learn==1.5.2
xgboost==2.1.3
scipy==1.13.1

# --- MLOps ---
mlflow==2.18.0
dvc==3.58.0

# --- RAG / vector DB ---
chromadb==1.0.15
sentence-transformers==3.3.1

# --- API layer (Phase 11) ---
fastapi==0.115.6
uvicorn==0.32.1
pydantic==2.13.4

# --- Local + cloud inference ---
# ollama>=0.6 required: google-genai needs httpx>=0.28.1, which conflicts
# with ollama==0.4.4's httpx<0.28 pin. Pin httpx explicitly so a fresh
# install doesn't have to re-discover this resolution.
ollama==0.6.2
google-genai==2.18.1
groq==1.6.0
httpx==0.28.1

# --- Utilities ---
python-dotenv==1.0.1
tqdm==4.67.1
pytest==8.3.4

# --- Notebook / EDA (Phase 1) ---
jupyter==1.1.1
matplotlib==3.9.3